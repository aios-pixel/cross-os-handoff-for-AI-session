interface:
  display_name: "Project Handoff"
  short_description: "Create, verify, and resume cross-host project handoffs"
  default_prompt: "Use $handoff with `handoff prepare`, `handoff after-task`, or `handoff resume` for this project."
policy:
  allow_implicit_invocation: true
